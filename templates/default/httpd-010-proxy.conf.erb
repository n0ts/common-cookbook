# Generated by Chef for <%= node['hostname'] %>.
# Local modifications will be overwritten.

<IfModule mod_proxy.c>
    ProxyRequests Off

    <Proxy *>
        AddDefaultCharset off
        Order deny,allow
        Deny from all
        Allow from <%= node['common']['apache']['allow_from'] %>
    </Proxy>

    ProxyVia On
</IfModule>
